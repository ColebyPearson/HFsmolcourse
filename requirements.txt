# Core fine-tuning stack
transformers>=4.45.0
trl>=0.12.0
peft>=0.13.0
datasets>=2.20.0
accelerate>=0.34.0
bitsandbytes>=0.43.0          # 4/8-bit quantization for SFT/DPO

# PyTorch (install matching CUDA wheel separately if needed)
torch>=2.4.0

# Tracking + metrics
wandb                          # optional; comment out if you don't track runs
evaluate>=0.4.0

# Vision (Unit 3 VLMs)
Pillow>=10.0.0
torchvision>=0.19.0

# Utilities
pyyaml>=6.0
huggingface_hub>=0.24.0
